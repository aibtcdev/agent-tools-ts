import { FetchFn } from '@stacks/network';
import { GaiaHubConfig } from '@stacks/storage';
import { Wallet } from './common';
export interface LegacyConfigApp {
    origin: string;
    scopes: string[];
    lastLoginAt: number;
    appIcon: string;
    name: string;
}
interface LegacyConfigIdentity {
    username?: string;
    address: string;
    apps: {
        [origin: string]: LegacyConfigApp;
    };
}
export interface LegacyWalletConfig {
    identities: LegacyConfigIdentity[];
    hideWarningForReusingIdentity?: boolean;
}
export declare function fetchLegacyWalletConfig({ wallet, gaiaHubConfig, fetchFn, }: {
    wallet: Wallet;
    gaiaHubConfig: GaiaHubConfig;
    fetchFn?: FetchFn;
}): Promise<LegacyWalletConfig | null>;
export {};
