import { FetchFn } from '@stacks/network';
import { PublicPersonProfile, PublicProfileBase } from '@stacks/profile';
import { GaiaHubConfig } from '@stacks/storage';
import { Account } from './common';
export declare const DEFAULT_PROFILE: PublicPersonProfile;
export declare const DEFAULT_PROFILE_FILE_NAME = "profile.json";
export declare const fetchProfileFromUrl: (profileUrl: string, fetchFn?: FetchFn) => Promise<PublicProfileBase | null>;
export declare const fetchAccountProfileUrl: ({ account, gaiaHubUrl, }: {
    account: Account;
    gaiaHubUrl: string;
}) => Promise<string>;
export declare function signProfileForUpload({ profile, account, }: {
    profile: PublicProfileBase;
    account: Account;
}): string;
export declare function uploadProfile({ gaiaHubUrl, account, signedProfileTokenData, gaiaHubConfig, }: {
    gaiaHubUrl: string;
    account: Account;
    signedProfileTokenData: string;
    gaiaHubConfig?: GaiaHubConfig;
}): Promise<void>;
export declare const signAndUploadProfile: ({ profile, gaiaHubUrl, account, gaiaHubConfig, }: {
    profile: PublicProfileBase;
    gaiaHubUrl: string;
    account: Account;
    gaiaHubConfig?: GaiaHubConfig | undefined;
}) => Promise<void>;
