import { Wallet } from './models/common';
export type AllowedKeyEntropyBits = 128 | 256;
export declare const generateSecretKey: (entropy?: AllowedKeyEntropyBits) => string;
export declare const generateWallet: ({ secretKey, password, }: {
    secretKey: string;
    password: string;
}) => Promise<Wallet>;
export declare const generateNewAccount: (wallet: Wallet) => {
    accounts: import(".").Account[];
    encryptedSecretKey: string;
    salt: string;
    rootKey: string;
    configPrivateKey: string;
};
