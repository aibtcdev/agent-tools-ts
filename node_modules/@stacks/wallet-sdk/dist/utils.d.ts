import { ChainID } from '@stacks/common';
import { FetchFn } from '@stacks/network';
import { GaiaHubConfig } from '@stacks/storage';
export declare function assertIsTruthy<T>(val: T): asserts val is NonNullable<T>;
export declare const getProfileURLFromZoneFile: (name: string, fetchFn?: FetchFn) => Promise<string | undefined>;
interface HubInfo {
    challenge_text?: string;
    read_url_prefix: string;
}
export declare const getHubInfo: (gaiaHubUrl: string, fetchFn?: FetchFn) => Promise<HubInfo>;
interface ConnectToGaiaOptions {
    hubInfo: HubInfo;
    privateKey: string;
    gaiaHubUrl: string;
}
export declare const connectToGaiaHubWithConfig: ({ hubInfo, privateKey, gaiaHubUrl, }: ConnectToGaiaOptions) => GaiaHubConfig;
export declare const makeGaiaAssociationToken: ({ privateKey: secretKeyHex, childPublicKeyHex, }: {
    privateKey: string;
    childPublicKeyHex: string;
}) => string;
interface WhenChainIdMap<T> {
    [ChainID.Mainnet]: T;
    [ChainID.Testnet]: T;
}
export declare function whenChainId(chainId: ChainID): <T>(chainIdMap: WhenChainIdMap<T>) => T;
export {};
